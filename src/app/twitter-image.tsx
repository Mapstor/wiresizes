// Twitter card image — same generator as opengraph-image so Twitter and
// other platforms that prefer twitter:image stay in sync with og:image.
export { default, alt, size, contentType } from './opengraph-image';
